# Copie para .env e preencha. O .env fica fora do git.
OPENAI_API_KEY=
# Opcional — fallback declarado no routing.yaml dos tenants.
DEEPSEEK_API_KEY=
# Token do bot, do BotFather. Nunca commitar.
TELEGRAM_BOT_TOKEN=
